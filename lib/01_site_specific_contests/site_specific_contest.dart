import 'package:flutter/material.dart';

class SiteSpecific extends StatelessWidget {
  const SiteSpecific({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This page will show site specific contest details."),
    );
  }
}
