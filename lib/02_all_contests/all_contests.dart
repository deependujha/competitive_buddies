import 'package:flutter/material.dart';

class AllContests extends StatelessWidget {
  const AllContests({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This page will show all contest details."),
    );
  }
}
